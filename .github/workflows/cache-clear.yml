name: "Cleanup caches by a branch"

on: # yamllint disable-line rule:truthy
    pull_request:
        types:
            - "closed"

jobs:
    cleanup-branch-cache:
        uses: "anolilab/workflows/.github/workflows/cleanup-branch-cache.yaml@9c000ebdb7571f022d19752c1c38a57e61a63448" # main
        with:
            target-repo: "anolilab/javascript-style-guide"
