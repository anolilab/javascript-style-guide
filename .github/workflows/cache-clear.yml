name: "Cleanup caches by a branch"

on: # yamllint disable-line rule:truthy
    pull_request:
        types:
            - "closed"

jobs:
    cleanup-branch-cache:
        uses: "anolilab/workflows/.github/workflows/cleanup-branch-cache.yaml@8249d8ea27b21190f8079df71fd7ba0c63f8d352" # v20.2.10 # main
        with:
            target-repo: "anolilab/javascript-style-guide"
